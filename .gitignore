node_modules
.next
out
.env
.env.*
!.env.example
.DS_Store
dist
coverage
.vercel

# typescript / next
*.tsbuildinfo
next-env.d.ts

# graphify knowledge-graph tooling artifacts (never commit)
# Covers graph.json, manifest.json, GRAPH_REPORT.md, graph.html, cache/, cost.json,
# and dated snapshots (graphify-out/<date>/), plus the local skill copy.
graphify-out/
.claude/skills/graphify/

# local attachment blob store (LocalDiskStorage) — never commit uploaded files
var/

# Windows packaging output (never commit the zip or bundled binaries)
*.zip
/packaging/windows/dist/
/vendor/
